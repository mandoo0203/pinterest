<h2>All boards </h2>

<% @boards.each do |board| %>
  <div>
    <h2><%= board.name %></h2>
    <p><%= link_to 'edit', edit_board_path(board) %> <%= link_to 'delete', board, method: :delete %>
  </div>
<% end %>
